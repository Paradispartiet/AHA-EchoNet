name: Insight Synthesis V2 calibrated live gold temp

on:
  pull_request:
    branches:
      - main
    paths:
      - scripts/insight-synthesis-v2-calibrated-live-gold-temp.cjs
      - .github/workflows/insight-synthesis-v2-calibrated-live-gold-temp.yml

permissions:
  contents: read

jobs:
  measure:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Measure calibrated deployed V2
        run: node scripts/insight-synthesis-v2-calibrated-live-gold-temp.cjs
      - name: Upload calibrated V2 measurement
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: insight-synthesis-v2-calibrated-live-gold
          path: v2-calibrated-live-results/*.json
          if-no-files-found: error
          retention-days: 2
