name: TEMP dispatch AHA quota smoke once

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  actions: write

jobs:
  dispatch:
    if: github.head_ref == 'agent/v2-quota-code-smoke-dispatch-once'
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch paid smoke on canonical main
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          body='{"ref":"main","inputs":{"live_mode":"smoke","live_cost_ack":"RUN_AHA_LIVE_SMOKE"}}'
          curl --fail-with-body --silent --show-error -X POST \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/aha-projection-product-browser-evaluation-v2.yml/dispatches" \
            -d "${body}"
          echo "Paid smoke dispatch accepted."
